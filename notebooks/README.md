# Notebooks

This folder is reserved for optional exploratory notebooks. The core project is implemented as reproducible Python scripts in `src/` so it can run cleanly from the command line and in CI.
